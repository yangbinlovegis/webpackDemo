const initState = [
	{
		text:'webpack react redux deom',
		completed: false,
		id:0
	}
]

export default function reducer(state=initState, action) {
	switch (state.type) {
		case '1':
            break;
        default:
		    return state;
	}
}